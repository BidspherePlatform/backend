package com.bidsphere.bidsphere.types;

public enum ErrorCode {
    NOT_ALLOWED,
    INVALID_SESSION,
    INVALID_CREDENTIALS,
    INVALID_USER,
    USER_EXISTS,
    INVALID_IMAGE,
    UPLOAD_FAILED,
    LISTING_CREATION_FAILED,
    INVALID_LISTING
}
